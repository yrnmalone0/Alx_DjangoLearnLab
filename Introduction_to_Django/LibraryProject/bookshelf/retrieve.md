# Retrieve and display all attributes of the book
To retrieve and display all attributes of a book instance in Django, you can use the Django shell or create a view to handle the retrieval of book instances. Below are the command and output from Django shell in this module.

**Python Command:**
```
Book.objects.all()

```


**Expected Output:**
```
<QuerySet [<Book: 1984>]>

```